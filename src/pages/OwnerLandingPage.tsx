import {Container, Typography} from "@mui/material";

const OwnerLandingPage = () => (
    <Container sx={{mt: 10}}>
        <Typography variant="h4">Owner Dashboard Coming Soon...</Typography>
    </Container>
);
export default OwnerLandingPage;
